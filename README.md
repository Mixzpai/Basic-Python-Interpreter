# Basic-Python-Interpreter
A Python interpreter for math expressions. It supports integers, arithmetic (+, -, *, /, %, ^), square root, log2, log10, comparisons (&lt;, >, &lt;=, >=, ==, !=), and booleans. Input is tokenized, parsed, and evaluated.
